{
  "name": "github-downloads-action",
  "version": "1.0.1",
  "private": true,
  "description": "Track release downloads with a GitHub Action that writes badge-ready JSON and optional SVG charts.",
  "main": "src/index.js",
  "scripts": {
    "lint:style": "node scripts/style-check.js",
    "test": "npm run lint:style && node tests/run-tests.js && node compat/run-compat.js",
    "check:release": "node scripts/release-check.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}

